import { TokenResponseAdmin } from './token-response-admin';

describe('TokenResponseAdmin', () => {
  it('should create an instance', () => {
    expect(new TokenResponseAdmin()).toBeTruthy();
  });
});
